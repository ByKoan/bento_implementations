# Bento integration in IoT Template

- ***Project Structure***

    -> `\app` (Main files for the project)

    -> `\app\mqtt` (Files for mqtt listener)
    
    -> `\app\core` (Files to transform the data)

    -> `\app\api` (Files for fastapi to the service of MQTT)

    -> `\tests` (Files for make test to the modules and functions)

    -> Dockerfile
    
    -> docker-compose.yml

    -> requirements.txt
